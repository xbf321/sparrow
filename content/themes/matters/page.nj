{% extends "./_layout.nj" %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <article class="post">
        <h1 class="post__title">{{ post.title }}</h1>
        <div class="post__content">
            {{ post.content | safe}}
            <br />
            -- EOF --
        </div>
    </article>
    {% include "./partials/comment.nj" %}
{% endblock %}
