{% extends "./_layout.nj" %}
{% block header %}
    <link rel="stylesheet" href="{{ ctx.loadManifest('admin.css') }}" type="text/css" media="all"/>
    <script>
        window.g_config = {
            settings: {{ settings | safe }},
            user: {{ userInfo | safe }}
        };
    </script>
{% endblock %}
{% block footer %}
    <script src="{{ ctx.loadManifest('admin.js') }}"></script>
{% endblock %}